## Neural Network
- Traditional ML often requires "feature engineering," where you manually guess and define complex mathematical terms to match the data's shape.
- In contrast, neural networks, by stacking simple linear layers with non-linear activations like ReLU, the model automatically discovers and assembles these complex functions during training. -> **approximate any complex function**

## Matrix
In a neuron network layer, we have $m$ inputs and $n$ outputs. So we need Jacobians Matrix

## Backpropagation Algorithm

